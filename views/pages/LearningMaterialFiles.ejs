<!DOCTYPE html>
<html>
	<head>
		<% include ../partials/head %>
	</head>
	<body>
		<header>
			<% include ../partials/header %>
		</header>
		<a href="/LearningMaterial/<%= items[0]['post_id'] %>/delete"><button class="btn btn-danger">Submit</button></a>
		<div class="container navbarMaterial">
			<% include ../partials/navbarFiles %>
		</div>
		<div class="container">
			<% if (join === undefined || join.length == 0) { -%>
				<div class="nofileTitle">
					<h5 class="">There are no available files...</h5>	
				</div>
			<% } else { -%>
				<% for (var i = 0; i < join.length; i++) { -%>
					<h4><%= join[i]['filename'] %></h4>
					<a href="/download/<%= join[i]['file_id']%>"><button class="btn btn-outline-success"><i class="fas fa-download"></i></button></a>
				<% } -%>
			<% } -%>

			
		</div>
	</body>
</html>